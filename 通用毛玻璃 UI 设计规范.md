# 通用毛玻璃 UI 设计规范

版本：1.0  
适用范围：Web 应用、桌面 Web 容器、响应式管理后台与内容型产品  
定位：框架无关的设计与工程基线，可用于 React、Vue、Svelte 或原生 Web 项目

## 1. 使用说明

本规范用于在不同项目中建立统一、稳定且可配置的 Glassmorphism（毛玻璃）界面。它定义的是一套材质系统，而不是某个组件库或一组固定白色透明背景。

使用时分为两部分：

- **必须遵守**：分层模型、透明度定义、玻璃与动画隔离、可读性、浏览器降级和无障碍要求。
- **按项目替换**：品牌字体、强调色、业务状态色、组件尺寸、页面密度和具体动效。

建议每个项目在本规范之外维护一份“项目扩展”，只记录品牌映射和业务组件差异，不复制或修改核心规则。

## 2. 设计目标

高质量毛玻璃 UI 应同时具备：

- 半透明雾面玻璃质感，背景能真实透出并产生柔焦。
- 极细高光描边与轻微镜面反射，表现边缘厚度。
- 克制、柔和的阴影，表现悬浮高度而不是制造灰色脏边。
- 清晰的信息层级，玻璃不能牺牲文字与控件可读性。
- 静止、悬浮、滚动和动画状态下材质一致。
- 浅色、深色、用户背景图片和不支持滤镜的浏览器均可使用。

推荐的视觉关键词：

```text
frosted glass UI, quiet luxury, soft negative space,
translucent surface, optical depth, subtle specular highlight,
gentle shadow, calm and airy composition, modern interface
```

### 2.1 核心原则

1. **先有背景，再有玻璃**：没有明暗或色彩变化的背景，模糊难以被感知；但背景不应抢夺内容注意力。
2. **玻璃是一种材质，不是一种颜色**：它由透明叠色、背景模糊、饱和度、亮度、高光、边框和阴影共同组成。
3. **材质与内容分层**：玻璃层只负责光学效果，内容始终位于其上方。
4. **层级来自同源参数**：强玻璃、常规玻璃、内嵌玻璃和控件玻璃共享同一基础参数，通过有限偏移建立差异。
5. **动画不更换材质**：动画过程中只插值几何和光学参数，不切换临时白底或另一套变量。
6. **可读性优先**：当背景与玻璃效果冲突时，应降低背景复杂度或提高表面不透明度。

## 3. 视觉层级

界面由后向前使用以下层级：

| 层级 | 名称 | 职责 | 示例 |
| --- | --- | --- | --- |
| 0 | Background | 提供色彩、环境光与景深 | 纯色、柔和光晕、用户图片 |
| 1 | Strong Glass | 承载高优先级悬浮内容 | 导航、模态窗、侧边面板、菜单 |
| 2 | Surface Glass | 承载常规内容 | 卡片、内容分区、工具栏 |
| 3 | Inset Glass | 表达玻璃内部的嵌入层 | 列表行、信息分组、评论、子面板 |
| 4 | Control Glass | 承载小型交互 | 按钮、输入框、选择器、开关 |
| 5 | Content | 传递信息与状态 | 文字、图标、标签、焦点环 |

规则：

- 同一区域最多执行一次真实 `backdrop-filter`。
- 嵌套层优先使用透明叠色、边框与内阴影，不重复模糊背景。
- 页面背景必须连续，除非产品结构明确需要，不使用纯色横条将背景切断。
- 浮层的层级通过材质强度和阴影表达，不通过不断提高白色不透明度表达。

## 4. 设计令牌架构

### 4.1 三层令牌

```text
Primitive 原始令牌
  ↓
Semantic 语义令牌
  ↓
Component 组件令牌
```

| 层级 | 内容 | 修改时机 |
| --- | --- | --- |
| Primitive | 原始色值、间距、字号、圆角、模糊、阴影 | 建立或整体升级系统时 |
| Semantic | 背景、文字、强调、危险、玻璃表面等用途 | 主题或品牌切换时 |
| Component | 卡片、按钮、输入框、弹窗的局部映射 | 调整单一组件时 |

### 4.2 命名规则

```text
--{category}-{item}-{variant}-{state}
```

示例：

```css
/* Primitive */
--blur-5: 22px;
--radius-4: 16px;

/* Semantic */
--material-glass-blur: var(--blur-5);
--color-text-primary: var(--neutral-950);

/* Component */
--card-glass-background: var(--material-glass-surface);
--button-glass-border-hover: var(--material-glass-border-strong);
```

组件不得直接引用无语义的硬编码色值。深色模式和品牌主题只覆盖语义层，避免逐个重写组件。

## 5. 中性起始主题

以下参数是一套适合大多数生产力、内容和工具型产品的起始值。品牌项目可以替换强调色，但应保留中性色对比关系。

### 5.1 浅色主题

| 语义 | 起始值 |
| --- | --- |
| 页面背景 | `#EEF1F2` |
| 主文字 | `#1B1E20` |
| 次文字 | `#596269` |
| 三级文字 | `#737D83` |
| 说明文字 | `#939CA2` |
| 强调色 | `#607B96` |
| 强强调色 | `#7896B3` |
| 成功 | `#5D806E` |
| 警告 | `#A77E47` |
| 危险 | `#B45F5F` |
| 信息 | `#607B96` |

### 5.2 深色主题

| 语义 | 起始值 |
| --- | --- |
| 页面背景 | `#111315` |
| 主文字 | `#F0F2F3` |
| 次文字 | `#C1C7CA` |
| 三级文字 | `#A0A8AD` |
| 说明文字 | `#828C92` |
| 强调色 | `#8DA6BB` |
| 强强调色 | `#A2B8CA` |
| 成功 | `#82A692` |
| 警告 | `#C19B68` |
| 危险 | `#D18484` |
| 信息 | `#8DA6BB` |

### 5.3 品牌色接入

- 品牌色只通过 `--color-accent` 等语义令牌接入。
- 大面积玻璃不直接使用高饱和品牌色。
- 状态染色建议将品牌或业务色控制在 6%–24% 透明度。
- 悬浮边框可混入 24%–36% 主题色，默认边框控制在 12%–22%。
- 业务状态必须同时使用文字或图标，不能只用颜色区分。

## 6. 背景规范

### 6.1 推荐背景

优先级从高到低：

1. 纯净、低饱和的中性色。
2. 具有少量大尺度柔焦光晕的静态渐变。
3. 经过压暗、降饱和和裁切处理的用户图片。

背景应满足：

- 不包含高频复杂纹理、密集文字或强对比图案。
- 光晕尺度至少为视口短边的 30%，避免出现小色块。
- 默认饱和度保持克制；动态背景不作为基础依赖。
- 若使用微噪点，不透明度建议不超过 2%。

### 6.2 用户背景图片

- 使用 `center / cover / no-repeat`，不拉伸。
- 明确文件类型、容量、隐私与持久化方式。
- 应提供移除图片和恢复默认背景的入口。
- 上传图片后仍需实时检查文字和控件对比度。
- 本地优先的应用建议只保存 Data URL 或本地引用，不自动上传服务器。

## 7. 透明度定义

设置项必须明确区分“背景透出比例”和“玻璃表面不透明度”。本规范推荐向用户展示前者。

```text
T = 用户看到的玻璃透明度，即背景透出比例
A = 玻璃表面颜色的不透明度
A = 1 - T
```

推荐默认值：

```text
T = 58%
A = 42%
```

建议范围：

| 参数 | 默认值 | 建议范围 | 步进 |
| --- | --- | --- | --- |
| 玻璃透明度 T | 58% | 10%–80% | 1% |
| 背景模糊 | 22px | 0–32px | 1px |

透明度越高，背景越清晰；玻璃表面颜色的实际不透明度越低。不得直接把用户设置的 58% 写成白色背景的 alpha 0.58。

## 8. 玻璃材质参数

### 8.1 浅色光学原语

```css
:root {
  --glass-opacity: 0.42;
  --glass-blur-amount: 22px;

  --glass-surface-rgb: 249 251 252;
  --glass-strong-rgb: 252 253 253;
  --glass-accent-rgb: 232 238 241;

  --glass-filter:
    blur(var(--glass-blur-amount))
    saturate(118%)
    brightness(1.025);

  --glass-control-filter:
    blur(var(--glass-blur-amount))
    saturate(114%)
    brightness(1.02);

  --glass-border: rgb(255 255 255 / 0.66);
  --glass-border-subtle: rgb(255 255 255 / 0.42);
  --glass-border-control: rgb(255 255 255 / 0.60);
}
```

### 8.2 深色光学原语

```css
[data-theme="dark"] {
  --glass-surface-rgb: 29 32 35;
  --glass-strong-rgb: 34 37 40;
  --glass-accent-rgb: 51 58 62;

  --glass-border: rgb(255 255 255 / 0.16);
  --glass-border-subtle: rgb(255 255 255 / 0.095);
  --glass-border-control: rgb(255 255 255 / 0.14);
}
```

### 8.3 四级材质

| 材质 | 基础 Alpha | 模糊 | 用途 |
| --- | --- | --- | --- |
| Strong | `clamp(0.28, A + 0.16, 0.88)` | 100% 基准 | 导航、弹窗、菜单 |
| Surface | `A` | 100% 基准 | 卡片、内容面板 |
| Inset | `clamp(0.12, A - 0.20, 0.54)` | 默认不重复模糊 | 内嵌分组、列表行 |
| Control | `clamp(0.16, A - 0.16, 0.60)` | 100% 或 80% 基准 | 输入框、按钮、选择器 |

材质的差异应保持在一个窄区间。Strong 不是不透明白色，Inset 也不是另一张白卡片。

### 8.4 镜面高光

```css
--glass-specular: linear-gradient(
  148deg,
  rgb(255 255 255 / 0.30) 0%,
  rgb(255 255 255 / 0.08) 34%,
  rgb(218 228 232 / 0.05) 66%,
  rgb(255 255 255 / 0.16) 100%
);

--glass-specular-strong: linear-gradient(
  148deg,
  rgb(255 255 255 / 0.34) 0%,
  rgb(255 255 255 / 0.10) 38%,
  rgb(218 228 232 / 0.06) 68%,
  rgb(255 255 255 / 0.18) 100%
);
```

规则：

- 同一页面的镜面光方向保持一致。
- 高光只表现光学厚度，不用于装饰性彩色渐变。
- 深色模式将各高光停靠点控制在约 2.5%–11%。
- 高光边缘应细腻，不能形成明显白色描边框。

### 8.5 阴影与厚度

```css
--glass-highlight:
  inset 0 1px 0 rgb(255 255 255 / 0.82),
  inset 1px 0 0 rgb(255 255 255 / 0.28),
  inset 0 -1px 0 rgb(96 114 122 / 0.07);

--glass-shadow-surface:
  0 18px 50px rgb(67 82 90 / 0.10),
  0 5px 16px rgb(67 82 90 / 0.055),
  0 1px 2px rgb(67 82 90 / 0.04);

--glass-shadow-inset:
  0 12px 32px rgb(67 82 90 / 0.065),
  0 2px 8px rgb(67 82 90 / 0.035);

--glass-shadow-modal:
  0 32px 90px rgb(49 62 69 / 0.18),
  0 8px 26px rgb(49 62 69 / 0.09);
```

阴影应宽、柔和、低对比。禁止纯黑硬阴影、明显外发光或大面积灰雾。

## 9. 排版规范

### 9.1 字体族

默认使用系统无衬线字体；品牌字体可替换首选项，但必须保留中文和系统回退：

```css
--font-sans: Inter, "SF Pro Text", -apple-system, BlinkMacSystemFont,
  "Segoe UI", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei",
  Arial, sans-serif;

--font-mono: "SF Mono", "JetBrains Mono", Consolas,
  "Liberation Mono", Menlo, monospace;
```

- 正文启用抗锯齿。
- 数字、金额、日期使用 `font-variant-numeric: tabular-nums`。
- 等宽字体只用于代码、编号和极短技术标识。

### 9.2 通用字号层级

| 令牌 | 字号 / 行高 | 字重 | 字间距 | 用途 |
| --- | --- | --- | --- | --- |
| Display | 32 / 40px | 600–650 | -0.015em | 页面核心表达 |
| H1 | 24 / 32px | 600 | -0.01em | 页面标题 |
| H2 | 20 / 28px | 580–600 | -0.005em | 大分区标题 |
| H3 | 16 / 24px | 560–600 | 0 | 卡片或弹窗标题 |
| Body | 14 / 22px | 400–470 | 0.008em | 默认正文 |
| Body Small | 13 / 20px | 400–470 | 0.012em | 紧凑界面正文 |
| Label | 12 / 18px | 500–570 | 0.025em | 控件、分区标签 |
| Caption | 11 / 16px | 400–500 | 0.02em | 辅助说明、元信息 |
| Eyebrow | 10 / 14px | 600 | 0.12–0.16em | 极短英文或编号 |

规则：

- 玻璃背景会降低边缘对比，正文不得依赖极细字重。
- 中文大段正文不使用明显加宽的字间距。
- 小于 12px 的文字只用于非关键辅助信息。
- 通过留白和字重建立层级，不依赖大量字号变化。

## 10. 间距、圆角与尺寸

### 10.1 间距系统

使用 4px 基准，并保留 2px 半步：

```css
--space-0: 0;
--space-0-5: 2px;
--space-1: 4px;
--space-1-5: 6px;
--space-2: 8px;
--space-2-5: 10px;
--space-3: 12px;
--space-3-5: 14px;
--space-4: 16px;
--space-5: 20px;
--space-6: 24px;
--space-7: 28px;
--space-8: 32px;
--space-10: 40px;
--space-12: 48px;
--space-16: 64px;
```

优先使用 16–24px 的卡片内边距和 24–48px 的分区间距，保持安静、空气感和可扫描性。

### 10.2 圆角系统

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| Small | 6px | 小标签、紧凑控件 |
| Control | 10px | 输入框、选择器 |
| Surface | 16px | 卡片、面板、弹窗 |
| Large Surface | 24px | 大型内容容器、移动端面板 |
| Pill | 999px | 胶囊按钮、圆形数量标记 |

圆角应与组件尺寸匹配。所有伪元素和内部玻璃层必须继承外层圆角。

### 10.3 控件尺寸

| 尺寸 | 高度 | 字号 | 图标 | 使用场景 |
| --- | --- | --- | --- | --- |
| Compact | 32px | 12–13px | 14px | 高密度工具栏 |
| Default | 40px | 14px | 16px | 默认表单与按钮 |
| Large | 48px | 15–16px | 18–20px | 移动端或主操作 |
| Icon | 最小 32px | — | 14–16px | 纯图标操作 |

触控设备的实际点击区域不得小于 44 × 44px，可通过透明外部热区实现。

## 11. 图标规范

### 11.1 视觉语言

- 使用统一的线性图标集，不混用线性、填充、拟物和 Emoji。
- 建议 `viewBox="0 0 16 16"` 或 `0 0 24 24`，同一产品固定一种坐标体系。
- `fill="none"`，`stroke="currentColor"`。
- 16px 图标线宽建议 1.3–1.6px；24px 图标线宽建议 1.5–2px。
- 使用圆头与圆角连接，避免在透明表面上产生尖锐噪点。

### 11.2 尺寸与语义

| 尺寸 | 用途 |
| --- | --- |
| 12px | 箭头、排序、极小辅助状态 |
| 14px | 表格操作、关闭、回复 |
| 16px | 默认操作与导航 |
| 20–24px | 空状态、移动端导航 |
| 32px 以上 | 品牌或插画图标，不属于控件图标 |

- 图标颜色继承语义文字色，不在 SVG 内硬编码主题色。
- 纯图标按钮必须提供 `aria-label` 或可见 Tooltip。
- SVG 使用 `aria-hidden="true"`，语义由外部按钮承担。
- 品牌 Logo 可以多色，但应单独维护，不作为 UI 图标样式来源。

## 12. 组件材质规范

### 12.1 组件映射

| 组件 | 推荐材质 | 关键规则 |
| --- | --- | --- |
| 顶部导航 / 工具栏 | Strong | 与背景四周留出间距，形成悬浮感 |
| 侧边栏 | Strong 或 Surface | 不使用不透明白色整栏 |
| 内容卡片 | Surface | 可做低饱和主题染色 |
| 模态窗 / 抽屉 | Strong | 配合独立背景遮罩 |
| 内嵌信息分组 | Inset | 默认不重复背景模糊 |
| 按钮 / 输入框 / 选择器 | Control | 统一交互状态，不切换实色底 |
| Dropdown / Popover | Strong | 保持足够内容对比度 |
| Tooltip | Strong | 可提高不透明度，避免小字不清晰 |
| 表格行 | Inset 或透明 | Hover 使用透明叠色 |
| 标签 / Badge | 边框或轻染色 | 颜色用于语义，不制造小白块 |

### 12.2 按钮

| 状态 | 材质变化 | 几何变化 |
| --- | --- | --- |
| Default | Control Glass | 无 |
| Hover | 边框与高光轻微增强 | `translateY(-1px)` |
| Active | 阴影收敛 | `translateY(0) scale(0.98)` |
| Focus | 保持原材质并增加焦点环 | 无 |
| Disabled | 不透明度 45%–50% | 禁止位移 |
| Loading | 内容降至 70%，显示进度 | 禁止重复点击 |

- Primary 通过强调色文字、边框或极轻染色表达，不使用高饱和实色填充。
- Destructive 使用危险色文字与边框；仅在确需强警示时使用低透明度危险色染色。
- 同一主题中的按钮不得混用玻璃悬浮和实色扩散动画。

### 12.3 输入框

- 使用 Control Glass，默认高度 40px，紧凑界面可用 32px。
- 聚焦时保持玻璃背景，只增加强调色边框和外部焦点环。
- Placeholder 使用说明文字色，但对比度仍需可辨认。
- 错误态同时显示危险色边框、图标或说明文字。
- 禁用态使用 `disabled` 属性，不只依赖降低透明度。

推荐焦点样式：

```css
.control:focus-visible {
  outline: 2px solid var(--color-focus);
  outline-offset: 2px;
}
```

### 12.4 卡片与内容面板

- 标准卡片使用 16px 圆角、16–24px 内边距和 Surface Glass。
- Hover 可以提升边框、阴影和 1–2px 位移，不改变玻璃基础透明度。
- 主题染色使用透明渐变叠加，推荐起点 12%–24%、终点 4%–8%。
- 卡片内部的子区域不使用纯白背景；需要分层时使用 Inset Glass。

### 12.5 模态窗与抽屉

- 外壳使用 Strong Glass，内部滚动内容不得覆盖玻璃伪元素。
- Header、Body、Footer 使用细分隔线或透明叠色，不拆成三张白色卡片。
- 背景遮罩与弹窗是两个独立层；遮罩负责背景聚焦，弹窗负责内容材质。
- 小屏幕下保持 16px 安全边距，并允许转换为底部 Sheet 或近全屏面板。

### 12.6 标签与 Badge

- 默认采用无背景的边框式标签，文字与边框共享语义色。
- `padding: 2px 8px`，字号 11–12px，圆角 999px 或 8–10px。
- 需要强调时可加入不超过 10% 的同色染色。
- 颜色较浅时自动校正明度，保证文字对比度。

## 13. 交互状态优先级

状态冲突时使用以下优先级：

```text
Disabled > Loading > Active > Focus > Hover > Default
```

通用过渡：

```css
--duration-fast: 100ms;
--duration-standard: 200ms;
--duration-slow: 300ms;

--ease-standard: cubic-bezier(0.4, 0, 0.2, 1);
--ease-spring: cubic-bezier(0.16, 1, 0.3, 1);
```

| 属性 | 时间 | 缓动 |
| --- | --- | --- |
| 颜色、边框、透明度 | 100–150ms | Standard |
| 阴影、位移、缩放 | 180–220ms | Standard 或 Spring |
| 弹窗进入与退出 | 240–360ms | Standard |
| 复杂空间形变 | 500–700ms | Standard |

避免对 `all` 做长时间 transition，应明确列出需要插值的属性。

## 14. 3D 与形变动画

### 14.1 材质一致性

复杂卡片翻转、共享元素转场或窗口形变必须遵守：

- 动画起点、过程和终点引用同一套玻璃语义令牌。
- 变换发生在外层 wrapper，玻璃材质位于内层元素或伪元素。
- 源元素在动画副本存在期间保持隐藏或占位，不能同时显示。
- 关闭动画结束前不恢复源元素，防止重影。
- 内嵌区域不得在动画中临时变为纯色背景。

### 14.2 动态模糊

动态模糊应连续经过“清晰 → 模糊峰值 → 清晰”，而不是中途突然开启和关闭：

| 进度 | 模糊参考 | 方向拉伸参考 |
| --- | --- | --- |
| 0% | 0 | 1 |
| 15% | 0.4px | 1.004 |
| 30% | 2.0–2.5px | 1.025 |
| 50% | 1.5–2.0px | 1.018 |
| 70% | 0.8–1.2px | 1.010 |
| 85% | 0.3–0.5px | 1.004 |
| 100% | 0 | 1 |

拉伸应沿物体运动或翻转方向执行：先旋转坐标轴，再执行 `scaleX`，最后反向旋转坐标轴。

### 14.3 背景聚焦

如果目标状态需要背景模糊，遮罩必须在完整转场内线性插值：

```text
进入：blur 0 → target，overlay alpha 0 → target
退出：blur target → 0，overlay alpha target → 0
```

不得在最后一帧才挂载、卸载或切换背景滤镜。建议目标背景模糊为 16–24px，浅色遮罩 14%–22%，深色遮罩 28%–40%。

## 15. 工程实现

### 15.1 伪元素隔离模式

推荐由伪元素独立承担玻璃采样：

```css
.glass {
  position: relative;
  isolation: isolate;
  background: transparent;
  border: 1px solid var(--glass-border);
  border-radius: var(--radius-surface);
}

.glass::before {
  content: "";
  position: absolute;
  inset: 0;
  z-index: -1;
  border-radius: inherit;
  background:
    var(--glass-specular),
    rgb(var(--glass-surface-rgb) / var(--glass-opacity));
  box-shadow: var(--glass-highlight), var(--glass-shadow-surface);
  -webkit-backdrop-filter: var(--glass-filter);
  backdrop-filter: var(--glass-filter);
}
```

### 15.2 强制约束

- 不在包含 `transform`、`will-change: transform`、`translateZ(0)` 或 3D 翻转的节点上直接设置 `backdrop-filter`。
- 变换 wrapper 与玻璃采样节点分离。
- 所有内容位于玻璃伪元素上方。
- 伪元素继承圆角，并被组件边界正确裁切。
- 不通过新增多个 `backdrop-filter` 解决层级问题。
- 动画克隆体必须继承主题作用域和全部玻璃变量。

这些约束用于规避 Chromium 合成层中的白块、闪烁、重影、透明度丢失和最后一帧材质跳变。

### 15.3 React / Vue 的职责边界

组件框架只负责：

- 保存主题开关、透明度、模糊和背景设置。
- 向根节点写入 CSS Variables 或主题属性。
- 管理弹窗、动画副本和源元素的生命周期。
- 响应 `prefers-color-scheme` 与降低动态效果偏好。

玻璃光学效果应主要由 CSS 完成，避免每个业务组件通过 JavaScript 计算独立色值。

### 15.4 是否使用组件库

通用 Glassmorphism 组件库适合静态原型，但复杂产品应谨慎使用。采用前检查：

- 是否支持统一主题令牌和动态透明度。
- 是否使用伪元素隔离滤镜。
- 是否能与 3D transform、Portal 和共享元素转场共存。
- 是否提供深色、降级和降低透明度方案。
- 是否允许移除内置白底、阴影和按钮填充动画。

如果需要大幅覆盖库的内部样式，直接使用设计令牌与 CSS 往往更简单、更稳定。

## 16. 不支持滤镜时的降级

```css
:root {
  --glass-fallback-surface: #f7f8fa;
  --glass-fallback-strong: #ffffff;
  --glass-fallback-inset: #eef2f7;
}

[data-theme="dark"] {
  --glass-fallback-surface: #2c2c2e;
  --glass-fallback-strong: #303034;
  --glass-fallback-inset: #292a2d;
}

@supports not ((backdrop-filter: blur(1px)) or
  (-webkit-backdrop-filter: blur(1px))) {
  .glass::before {
    background: var(--glass-fallback-surface);
  }
}
```

降级时仍保留圆角、边框、高光和阴影，不隐藏内容或功能。

## 17. 性能规范

- 只对需要玻璃效果的局部表面使用 `backdrop-filter`，避免整个长页面反复模糊。
- 尽量减少大面积、重叠和持续移动的模糊层。
- 移动端可将默认模糊从 22px 降至 14–18px。
- 不长期保留 `will-change`；只在动画前添加，结束后清理。
- 列表虚拟化时保持玻璃材质在稳定父级，避免每一行创建高成本滤镜。
- 动态背景、噪点和玻璃动画应分别评估 GPU 成本。
- 使用中低端设备和 Chromium、Safari 实机检查滚动与动画。

## 18. 响应式规范

- 小屏幕优先减少同时可见的玻璃层数量，不简单降低所有文字尺寸。
- 大面板可转换为近全屏 Sheet，并保留 12–16px 安全边距。
- 横向内容优先滚动或重排，关键卡片不压缩到不可阅读。
- 触控控件点击区域至少 44 × 44px。
- 背景图片保持 cover 裁切，并检查不同宽高比下的视觉焦点。
- 移动端可提高表面不透明度 4%–8%，补偿较小屏幕上的视觉噪声。

## 19. 无障碍规范

- 普通文字对比度至少 4.5:1；大号文字和 UI 边界至少 3:1。
- 焦点环至少 2px，外偏移 2px，并与背景保持 3:1 对比。
- 状态不能只通过颜色表达，应同时提供文字、图标或形状。
- 图标按钮提供无障碍名称；Loading 使用 `aria-busy`。
- 表单错误使用 `aria-invalid` 和关联说明。
- `prefers-reduced-motion: reduce` 时取消翻转、漂浮和共享元素形变。
- `prefers-reduced-transparency` 可用时，关闭背景模糊并提高表面不透明度。
- `prefers-contrast: more` 时提高边框和文字对比，减少背景透出。

## 20. 主题设置交互

推荐设置结构：

```text
主题
├── 外观模式：跟随系统 / 浅色 / 深色
└── 毛玻璃主题：关闭 / 开启
    ├── 玻璃透明度
    ├── 背景模糊
    ├── 背景图片
    └── 恢复默认
```

- 只有开启毛玻璃主题后才展开详细设置。
- 设置变化应实时预览，并提供明确的默认值。
- 背景图片上传需展示文件限制和删除入口。
- 关闭毛玻璃时保留用户参数，重新开启后恢复上次选择。
- 对性能较弱的设备可以提供“降低视觉效果”选项。

## 21. 禁止事项

- 禁止把所有玻璃统一写成 `rgba(255, 255, 255, 0.6)`。
- 禁止使用纯白内嵌区域破坏玻璃整体性。
- 禁止在变换节点上直接使用 `backdrop-filter`。
- 禁止同一区域叠加多个高强度背景模糊。
- 禁止动画结束前后切换不同透明度或不同主题作用域。
- 禁止以高饱和彩色渐变代替玻璃材质。
- 禁止用复杂背景掩盖玻璃参数不足。
- 禁止为了“高级感”使用过低对比度的细小文字。
- 禁止所有按钮同时具有玻璃动效和旧版实色填充动画。
- 禁止在不测试 Safari 与 Chromium 的情况下交付复杂 3D 玻璃动画。

## 22. 项目适配模板

新项目采用本规范时，至少完成以下映射：

```text
项目名称：
目标平台：
界面密度：宽松 / 标准 / 紧凑

品牌
- 主字体：
- 等宽字体：
- 强调色：
- 成功 / 警告 / 危险 / 信息色：

背景
- 默认浅色背景：
- 默认深色背景：
- 是否允许用户图片：
- 图片容量与存储方式：

玻璃
- 默认透明度 T：
- 默认模糊：
- Strong / Surface / Inset / Control 映射：
- 是否存在 3D 或共享元素动画：

组件
- 导航材质：
- 卡片材质：
- 弹窗材质：
- 表格 / 列表材质：
- 按钮尺寸与圆角：
- 标签样式：

兼容与无障碍
- 最低浏览器版本：
- 降级表面色：
- 降低动态效果方案：
- 降低透明度方案：
```

## 23. 验收清单

### 设计

- [ ] 背景低饱和且不会抢夺内容注意力。
- [ ] Strong、Surface、Inset、Control 四级材质肉眼可辨但保持同源。
- [ ] 高光、边框、阴影和模糊共同形成玻璃，而非白色透明块。
- [ ] 浅色、深色和用户背景下文字均可读。
- [ ] 组件没有残留不一致的实色背景和旧动画。

### 交互

- [ ] Hover、Active、Focus、Disabled、Loading 状态完整。
- [ ] 弹窗进入与退出时背景遮罩连续插值。
- [ ] 3D 或共享元素动画全过程材质一致。
- [ ] 动态模糊连续且与运动方向一致。
- [ ] 动画副本与源元素不会同时显示或产生重影。

### 工程

- [ ] 使用 Primitive → Semantic → Component 三层令牌。
- [ ] 变换节点与玻璃滤镜节点已分离。
- [ ] 不支持滤镜时存在可用回退。
- [ ] `will-change` 会在动画结束后清理。
- [ ] 通过 Chromium、Safari、移动端和中低性能设备验证。

### 无障碍

- [ ] 文字、UI 边界和焦点环达到最低对比度。
- [ ] 键盘可以完成全部主要操作。
- [ ] 图标按钮、错误和加载状态具有正确语义。
- [ ] 降低动态效果时没有功能缺失和布局跳变。
- [ ] 高对比度或降低透明度场景下界面仍可用。

## 24. 何时不应使用毛玻璃

以下场景应减少或关闭玻璃效果：

- 高密度数据表格需要最大化文字与网格对比度。
- 设备性能不足，滚动或动画无法稳定达到目标帧率。
- 背景完全单一且没有任何景深信息，模糊无法被感知。
- 产品所在环境要求极高可访问性或强制高对比度。
- 页面包含大量重叠浮层，玻璃会造成认知和渲染负担。
- 打印、导出 PDF 或截图需要确定的实色输出。

毛玻璃应服务于信息层级和空间关系，而不是成为每个项目必须套用的视觉滤镜。

## 25. 实施结论

可复用的毛玻璃系统依赖三个基础判断：

1. **把玻璃作为独立材质层**，由统一令牌和隔离伪元素承载。
2. **把动画与玻璃采样分开**，保证静止、变换和结束状态使用相同材质。
3. **把可读性和降级作为核心能力**，而不是视觉完成后的补充处理。

遵守这三点后，具体项目只需替换品牌语义、组件尺寸与业务状态映射，即可复用同一套毛玻璃设计语言。
