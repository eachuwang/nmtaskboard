# 更新日志

每个版本先介绍主要变化，再列出新增功能、体验改进与问题修复。

## v2.6.6 · 2026-09-21

### 看板搜索、成员改名与登录记忆

看板新增搜索框，按卡片全部文本即时筛选；工作区所有者可修改成员显示名称；登录「记住我」记住账号自动填入。

#### 新增功能

- 看板在排序、筛选旁新增搜索框：输入即筛选卡片，匹配标题、描述、标签、负责人与参与人姓名、状态（含自定义状态）、优先级、项目、父任务标题、截止日期、逾期与阻塞原因，不区分大小写。空格分隔多个关键词时需全部命中；「#编号」精确匹配任务编号；按「/」可把焦点移到搜索框。一键清除：输入框右侧清除按钮（有内容时出现）、按 Esc、无匹配提示浮层中的「清除搜索」。搜索与筛选、排序可叠加。
- 工作区所有者可为团队任一成员修改显示名称：在「团队」成员表格行内改名，登录名与邮箱不受影响；账号级显示名称在同一实例各工作区共用，任务负责人等展示同步更新，历史记录保留发生时名称。本人仍可经个人资料自改名，管理员与普通成员没有改名其他成员的权限。

#### 体验改进

- 没有匹配结果时显示提示浮层（附清除入口），不再呈现空看板；搜索框与排序、筛选按钮同规格（毛玻璃质感、图标与动效一致、与文字严格垂直居中）。
- 登录「记住我」在 30 天免登录之外同时记住登录账号：下次自动填入并把焦点移到密码框，取消勾选后清除记忆；密码不在应用侧存储明文，由浏览器密码管理器保存。

## v2.6.5 · 2026-09-21

### 看板排序与智能创建改进

看板新增排序按钮，默认按优先级从高到低排列；智能创建草稿支持指定负责人，生成后直接进入待办列。

#### 新增功能

- 看板在筛选旁新增排序按钮：默认按优先级从高到低，可切换截止日期（由近及远）、创建时间（由新到旧）或手动拖拽顺序；排序键支持方向子菜单（从低到高、由远及近、由旧到新）。看板与列表视图共用同一排序，偏好本地记忆；排序只调整展示，不改动拖拽记录的手动顺序。
- 智能创建草稿新增负责人下拉选择（默认创建人）：指定负责人后，卡片生成时直接进入待办列而非待整理列；清除负责人回落待整理，手动选择过状态则保持所选状态。

#### 体验改进

- AI 解析按钮悬浮在任务描述输入框右下角、与边框等距，为下方草稿表单腾出空间；草稿列表高度随窗口自适应，底缘贴合创建按钮上方的分隔线，不再挤占底部按钮区域。
- 创建弹窗与草稿卡的操作按钮统一为毛玻璃效果，悬浮时边框过渡到强调色；创建与「丰富编辑」按钮圆角统一为 8px；草稿卡字段标签统一灰色，负责人与状态下拉按钮严格水平对齐。

## v2.6.4 · 2026-09-21

### 报告生成提速，父子层级与编辑体验完善

报告保留自定义模板、父子层级和完整进展素材，减少生成等待；编辑区充分利用页面空间，生成完成后的核对提醒清晰呈现。

#### 体验改进

- 报告按父子任务组织内容：子任务放在父任务下；父子任务分属不同分节时，以父任务标题作为分组，不虚构父任务在该分节的进展。
- 报告编辑框填满内容区域，长报告在框内滚动，编辑和预览使用一致的可用高度。
- 报告生成减少重复素材和冗余提示，完整保留评论、回复及进展记录；使用百炼 DeepSeek V4 的已支持型号时，生成和润色关闭思考模式，缩短等待。

#### 问题修复

- 修复思考型模型持续返回数据时仍被总时长限制中断的问题：流式请求按连续无数据的时间判断超时，生成期间维持连接。
- 修复父任务不在当前报告范围内时子任务层级丢失的问题：在有读取权限且能取得父任务标题时，保留父级分组。
- 修复报告生成完成后成功提示与事实核对提醒重叠的问题：合并为一条提示，保留日期和数字核对信息。

## v2.6.3 · 2026-09-18

### 报告生成单一入口，指派回归详情页

报告生成合并为一条带代际守卫的生成路径：动画稳定显示直到完成，参数变更不再静默重置报告内容；任务详情重新提供快速指派，指派与取消对称，转移所有权默认退出负责人。

#### 体验改进

- 报告生成统一为单一入口，并发操作始终以最后一次为准：生成中的进度提示稳定显示直到完成，完成后正文与左侧任务清单同源更新。
- 调整报告参数（范围、模板、周期等）不再自动重置报告：内容保持不动并提示「参数已变」，点击「重新生成」按新参数发起；已编辑的内容不会被后台流程覆盖。
- 勾选剔除任务立即生效：取消勾选后按剔除后的清单自动重新生成，勾选状态在重新生成后保留。
- 报告预览与编辑所见即所得：预览按编辑框的实际换行渲染，模板的编号与缩进结构不再被折叠。
- 任务详情视图模式提供「指派任务」成员面板：点击成员即指派或取消指派，可将任务设为「未分派」；有指派权限即可对称管理所有负责人，包括操作者本人。
- 转移卡片所有权时，旧所有者默认退出负责人列表并失去编辑入口；在保存前重新勾选可显式保留参与。轨迹显示「将所有权转移给了 X，并退出了负责人」。
- 新建任务窗口对齐卡片详情的设计令牌：圆角、玻璃质感输入、字号与控件高度统一。
- 任务详情的附件分区增加「附件」标题，与子任务列表明确区分。

#### 问题修复

- 修复报告生成中的一组竞态：被中止的旧生成关闭新生成的进度提示、新旧响应乱序造成范围与内容错位、切换范围后预览只剩模板骨架且「重新生成」无响应。
- 修复「恢复原文」在生成后不可用的问题：现在回到最近一次生成的完整文本。
- 修复客户端断开连接时服务端误报「LLM 请求超时（300 秒）」：主动取消与真实超时分别处理，不再向已断开的连接写错误事件。
- 修复 AI 输出含证据外日期或数字时，核对警告未在页面上提示的问题。
- 修复报告生成遮罩层级：操作按钮不再浮在「AI 正在按模板生成」遮罩上方。
- 修复产品更名后健康检查断言仍检测「任务看板」的问题。

## v2.6.2 · 2026-09-17

### 更名「日新看板」，统一任务编辑草稿

产品更名为「日新看板」，界面与文档统一新名称；描述编辑直接使用任务草稿，展开收起不丢内容，离开未保存修改先确认，保存冲突只对双方都改过的字段提示选择。

#### 体验改进

- 产品更名为「日新看板」：登录页、浏览器标题、帮助、更新日志与文档统一使用新名称；仓库名、镜像名与部署配置保持不变，现有部署无需迁移。
- 丰富描述编辑器展开为全屏视图，与任务字段编辑同一份草稿：「返回任务」或 Esc 收起后内容保留，卡片「保存」统一提交；不再维护独立的本地描述草稿与恢复提示。
- 离开有未保存修改的任务前统一确认：关闭详情、切换页面或标签页、刷新或关闭浏览器都会先询问；保存进行中不能关闭。
- 保存冲突改为字段级核对：只对双方都修改的字段提供「保留我的修改」或「使用最新」选择，未涉及字段自动保留最新值。
- 新建任务的附件先创建任务再逐个上传：上传中断后重试只补齐剩余文件，不重复创建任务、不丢已传附件；部分完成时会提示已创建内容。
- 详情页「指派任务」快捷下拉并入编辑表单，负责人调整统一在「编辑卡片」中完成。

#### 问题修复

- 修复他人改动触发页面同步（SSE 刷新）时，正在编辑的草稿被重置的问题。
- 修复保存时未修改字段（如标题）覆盖他人并发修改的问题。
- 修复新建任务只填日期等字段时不作为未保存草稿提醒，误关闭丢失内容的问题。
- 修复删除前一条 AI 草稿后后一条草稿附件错配，以及取消新增文件被当作删除已有附件提交的问题。

## v2.6.1 · 2026-09-17

### 报告进展归纳：评论与记录一起讲清过程

报告生成把卡片评论及回复与进展记录一起作为进展素材，归纳成一句真实过程；「重新生成」先重读看板数据，缺失信息省略、不写占位话。

#### 新增功能

- 报告生成把卡片评论及回复纳入进展素材：与任务描述、进展记录一起按时间归纳成一句进展过程（如「经过三轮优化迭代后，准确率从 30% 提升至 95%」），而不是只写「当前处于进行中」；问题、建议和计划不会被写成已完成事实，已删除的评论不参与生成。

#### 体验改进

- 进展素材不再截断为最近三条：完整的评论与进展记录都会传给模型，长过程的多个阶段都能写进报告；只有评论与进展均无实际进展时，才描述任务当前状态。
- 「重新生成」先重新读取看板最新数据再生成，左侧任务列表随当前看板状态更新。

#### 问题修复

- 修复报告正文出现「无进展记录」「暂无」等陈述缺失的占位文字，以及内部状态码（in_progress、done）与优先级码（high、urgent）混入正文的问题。

## v2.6.0 · 2026-09-16

### 报告模板：按自己的骨架生成周报

报告改为自定义 Markdown 骨架模板——生成时把看板里的真实任务内容填进骨架；AI 润色只改表达，不动事实与结构。报告在页面间保留，生成与润色切换页面不中断。

#### 新增功能

- 报告支持自定义模板：模板就是你写的一段 Markdown 结构（分节、编号、层级由你决定，没有固定格式），可新建、编辑、删除，并保存为个人模板或（由管理员）工作区模板并设为默认。

#### 体验改进

- 报告按所选模板骨架生成：配置模型后把看板里的真实任务内容填入骨架，未配置模型时返回骨架可手动完善；个人默认模板优先于工作区默认。骨架的标题、编号与缩进保持原样，只替换占位符。
- 报告与生成内容在页面间保留：生成中或润色中切换页面不中断，返回后内容仍在；报告已存在时主按钮显示「重新生成」。
- AI 润色只润色文字表达：不改动事实，也不改动报告结构（分节标题、编号、缩进层级、条目数量与顺序保持不变）。
- 「管理模板」始终可从报告页工具栏进入，不再要求先生成报告；未编辑时模板窗口只显示模板列表，编辑时才展开表单。

#### 问题修复

- 修复自定义模板保存失败（模板 id 与数据库列类型不匹配）与模板列表读取为空的问题。

## v2.5.0 · 2026-09-15

### 丰富描述编辑器与优先级配色

用 Markdown、清单、图片和附件记录任务细节，卡片按优先级着色；同时完善所有权、报告与筛选体验。

#### 新增功能

- 任务描述支持富文本、Markdown 源码、分屏和预览模式，最多 50,000 个字符；提供字体、字号、颜色、高亮、标题、清单、表格、图片与附件。
- 描述编辑支持本地文字草稿、历史版本查看与恢复，以及保存冲突提示；完成编辑返回卡片草稿，保存卡片后提交。
- 任务所有者可以把所有权转移给现任负责人；非所有者的负责人不能把自己移出负责人列表。
- 看板与列表支持按状态、负责人、优先级、项目、标签和日期分组筛选。
- NM Helper 可以选择管理员已配置的模型，消息输入与对话展示更加清晰。

#### 体验改进

- 卡片按紧急红、高橙、中黄、低蓝着色，无优先级为无色半透明毛玻璃，底色不随任务状态改变。
- 卡片描述使用固定高度的非滚动预览，图片左对齐并等比例缩小；完整内容通过「展开阅读」查看。
- 工具栏将标题、段落和插入功能合并为下拉菜单，自适应换行；查找替换面板中文化并统一毛玻璃样式。
- 卡片阅读页移除重复的上传附件入口，保留已有附件查看和下载；帮助中心新增丰富描述使用指南。
- 时间型报告统一输出 Highlights、Details、In-progress、Plan for next week 四段，保留任务描述、进展摘要和父子层级。
- 所有权转移沿用结构化审计；旧指派接口与普通编辑保持相同的自我移除限制。

#### 问题修复

- 修复富文本编辑时图片引用丢失，保留未修改段落的 Markdown 原文，并为能匹配现有附件的旧图片引用提供恢复入口。
- 修复清单复选框在卡片表单中被拉伸、工具栏控件重叠、侧栏遮挡任务弹窗和关闭下拉菜单时误触发退出的问题。
- 描述保存按工作区统一加锁，只替换当前任务，避免覆盖其他任务的并发更新。

## v2.4.0 · 2026-09-10

### 自定义状态流程，让看板适应你的工作方式

为工作区定义自己的状态名称、顺序和生命周期。切换或删除列前先检查任务迁移；新的帮助中心让配置与日常使用更容易找到说明。

#### 新增功能

- 在「设置 → 任务状态」维护工作区唯一的自定义方案，也可以继续使用只读的默认七列。
- 为每列设置名称、状态值、颜色和生命周期：待实施、实施中、阻塞或终止态。终止态可选择已完成或不再实施。
- 保存前预览迁移目标、受影响任务与完成率变化。有多个匹配目标时，先选择再确认；删除列中的任务按原顺序迁到最近保留的列。
- 报告、NM Helper 和数据备份支持自定义状态。已删除的状态保留历史名称，并标注「已删除」。
- 帮助中心按主题组织内容，支持搜索、章节链接、页内目录与连续阅读。

#### 体验改进

- 状态名前的圆形标识使用毛玻璃质感，在看板、列表、详情、下拉选项和状态管理中保持一致。
- 自定义方案把实际完成与不再实施分开统计；流程迁移不会被当作本期实际完成，已保存报告保持原文。
- 更新日志按版本主题概括变化；README 重新整理首次登录、日常使用、部署和维护路径。

#### 问题修复

- 状态下拉浮层不再被容器裁剪或定位到视口之外；默认方案的状态控件保持只读。
- 方案和任务在同一事务内保存；预览过期或助手草稿所用的方案发生变化时，要求重新确认。
- 修正首次登录、账号权限和备份说明中的过时内容，避免按旧入口操作。

## v2.3.0 · 2026-09-08

### 多人负责同一任务，协作变化实时同步

为同一任务安排多位负责人，按成员调整协作能力。任务更新会同步到在线同事，子任务创建和深色界面也更加顺手。

#### 新增功能

- 同一任务支持多位负责人，创建者可按成员配置指派、编辑和评论能力。
- 参与人从任务子树的负责人自动汇总，不再需要手动维护。
- 任务状态、内容、评论和子任务的变更会实时同步到同一工作区的在线成员。
- 从任务详情直接新建子任务，父任务自动锁定；新建任务的高级选项也可选择父任务。

#### 体验改进

- 编辑项目的表单重新排布，标签位于控件上方，按钮和字段高度保持一致。
- 卡片字段对齐，列表和表单中的按钮更容易点击；团队成员操作集中到「管理」菜单。
- 移除没有实际使用场景的人工状态校准入口。

#### 问题修复

- 深色模式下的面板、弹窗、控件和卡片统一使用暗色毛玻璃，避免出现实心浅色块。
- 评论作者名旁不再出现多余的数字 0。
- 任务列表默认展开子任务，避免折叠造成任务数与看板看起来不一致。
- 过长的父任务选项不再挤压相邻控件，下拉样式与整体界面保持一致。

## v2.2.3 · 2026-09-08

### 长标题也不会遮住任务关系

卡片中的负责关系移到右下角，标题和标识各有位置。

#### 体验改进

- 「我负责」「他人负责」等关系以卡面右下角水印展示；只读信息合并标注，长标题不再挤掉标识。

## v2.2.2 · 2026-09-08

### 角色下拉保持一致的玻璃质感

修正角色选项的默认白底，让弹层与主界面保持一致。

#### 问题修复

- 角色编辑下拉中的选项使用毛玻璃样式，不再显示浏览器默认的白底按钮。

## v2.2.1 · 2026-09-08

### 角色编辑弹层更容易打开和关闭

在列表底部编辑角色时，选项不会被裁剪，关闭方式也更加明确。

#### 问题修复

- 角色下拉支持点击空白处、按 Escape 或再次点击触发按钮关闭。
- 角色浮层不再被列表边界裁剪，并使用与全局一致的毛玻璃主题。

## v2.2.0 · 2026-09-08

### 团队管理、独立用户名与更完整的工作报告

成员管理集中到团队页。身份信息、项目关联和任务记录更清楚，报告可以带上更多工作的上下文。

#### 新增功能

- 新增「团队」页，集中查看和搜索成员、管理邀请、调整权限、移除成员与转移所有权。
- 新增分工角色，管理员可以维护角色并为成员选择多项；分工角色与工作区权限分别管理。
- 登录用户名与显示名、邮箱分开维护；修改显示名不影响登录，选择同名成员时可通过用户名区分。
- 任务详情支持调整父级，新建任务可以选择项目，项目编辑支持选择参与成员。
- 当时版本支持显式指定任务参与人；后续在 v2.3.0 改为从任务子树自动汇总。
- 报告加入任务描述、评论摘要和子任务层级，并支持 Markdown 编辑与预览。
- 审计与最近操作展示具体任务名称和变化内容，方便回查协作过程。

#### 体验改进

- 主页面与控制台使用更轻量的静态渐变背景，登录页保留动态光束。
- 列表、按钮和动画图标统一样式，团队页的内容与页面边界对齐。

#### 问题修复

- 修复经代理调用外部模型时的兼容性问题。
- 审计时间使用统一格式，设置下拉不再被上层内容遮挡。

## v2.1.0 · 2026-09-07

### 项目可以编辑，看板操作更加顺手

完善项目和子任务的关联方式，增加帮助与更新日志入口，并改善看板交互。

#### 新增功能

- 项目详情新增编辑入口，可调整名称、图标、描述、优先级、负责人与起止日期。
- 卡片展示从子任务汇总的参与人信息。
- 看板支持 Ctrl + 鼠标滚轮横向查看状态列。
- 侧边栏增加使用帮助和更新日志入口；系统开启减少动态效果时，应用会降低视觉效果强度。

#### 体验改进

- 降低背景动效开销，让看板和任务详情的打开、关闭更加流畅。
- 卡片中的长字段以单行省略展示，打开详情可查看完整内容。

#### 问题修复

- 子任务在创建和编辑时跟随父任务的项目归属，创建时默认继承父任务优先级。
- LLM、Git 平台和存储等出站请求支持 HTTP(S) 代理，适配无法直接连接公网的服务器。
- 编辑项目后会正确更新修改时间。

## v2.0.0 · 2026-09-05

### 以工作区组织任务与团队协作

重新组织导航、账号与协作空间，并提供服务器离线部署方式。

#### 新增功能

- 采用工作区导航和多标签页，在任务、项目、报告与设置之间切换。
- 引入工作区成员、角色和任务权限，使用本地账号与邀请流程参与协作。
- 提供包含应用与数据库镜像的 Linux 服务器离线部署包。
