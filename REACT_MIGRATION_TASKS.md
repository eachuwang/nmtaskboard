# React 渐进迁移任务清单

> 规格：[REACT_MIGRATION_SPEC.md](./REACT_MIGRATION_SPEC.md)
> 规则：一次只推进一个未完成阶段。阶段完成后由用户验收；获得明确批准前不提交、不推送、不创建或合并 PR。
> 视觉基准：所有 React 布局统一以迁移前的原生 `public/` 前端（原入口 [http://127.0.0.1:3301/](http://127.0.0.1:3301/)）为准；迁移只替换渲染引擎，不重构 UI。

状态：`[ ]` 未开始，`[~]` 进行中，`[x]` 已完成，`[!]` 阻塞。

## M0：规格与基线

- [x] M0.1 从本地最新 `develop` 创建 `feature/react-migration`。
  - 完成：当前分支为 `feature/react-migration`，基线包含 `2d893cf`。
- [x] M0.2 编写迁移规格。
  - 完成：目标、非目标、seam、顺序、验证和回滚均有唯一事实源。
- [x] M0.3 建立分阶段任务清单。
  - 完成：每个阶段都有可验证的完成标准。
- [x] M0.4 运行迁移前后端测试基线。
  - 完成：`npm test` 共 69 项通过、0 项失败。
- [x] M0.5 对齐远端基线。
  - 完成：用户确认按当前本地 `develop` 和工作分支继续实施；远端补齐 `2d893cf` 是首次发布前置条件。
- [x] M0.6 用户审阅并批准阶段 0 文档。
  - 完成：用户明确要求按规格开始实施。

## M1：React 基础设施

- [x] M1.1 记录运行时决定。
  - 完成：`engines.node` 提升到 `>=22.12`，本机 Node.js `24.18.0` 满足要求。
- [x] M1.2 添加最小依赖。
  - 完成：仅加入 React、React DOM、Vite、React Vite 插件、Vitest、jsdom 和 Testing Library 必需包。
- [x] M1.3 建立 `client/` 入口和单一 React root。
  - 完成：React 页面可渲染，不接管旧前端 DOM。
- [x] M1.4 配置开发代理和 `/next` 基础路径。
  - 完成：开发页面能通过相对 `/api` 请求访问 Express。
- [x] M1.5 配置生产构建和 Express `/next` 静态入口。
  - 完成：构建后直接访问及刷新 `/next/*` 均成功，`/` 保持旧版。
- [x] M1.6 建立请求错误、SSE 和浏览器能力的最小模块。
  - 完成：重复底层行为各自只有一个实现，页面只使用其 interface。
- [x] M1.7 建立前端测试和统一检查命令。
  - 完成：`npm run check` 通过后端 69 项、前端 14 项测试和生产构建；Vite 开发代理已用临时端口人工验证。
- [x] M1.8 用户验收 M1。
  - 完成：用户在 M1 入口可访问后明确要求继续 M2，视为通过验收。

## M2：应用外壳与主题

- [x] M2.1 迁移顶栏、导航和页面容器。
  - 完成：React 外壳接入顶栏、看板/报告导航、设置入口和页面容器，键盘快捷键保持一致。
- [x] M2.2 迁移三态主题。
  - 完成：跟随系统、深色、浅色和现有 `tb-theme` localStorage 偏好均正常，并响应系统主题变化。
- [x] M2.3 接入现有 token、字体和基础控件样式。
  - 完成：Geist 字体、现有冷灰/蓝色 token、响应式断点、焦点态和减少动态效果规则迁入 `client/src/styles.css`。
- [x] M2.4 增加外壳用户行为测试。
  - 完成：前端 6 项测试覆盖 API 健康状态、导航、快捷键、设置弹窗和主题持久化。
- [x] M2.5 用户验收 M2。
  - 完成：用户在可访问入口后要求继续未完成任务，视为通过 M2 验收并进入 M3。

## M3：报告视图

- [x] M3.1 迁移报告类型、日期范围和周期切换。
  - 完成：支持日报、周报、双周报、月报、季报、年报和离职交接报告；日期输入、本期、前后周期和周末开关均已接入。
- [x] M3.2 迁移摘要读取、分节和任务勾选。
  - 完成：通过 `POST /api/report/template` 读取摘要，按分节渲染任务，并支持勾选剔除后重新生成 Markdown。
- [x] M3.3 迁移 Markdown 编辑、复制和下载。
  - 完成：报告内容可直接编辑，复制使用浏览器能力 interface，支持下载 `.md` 文件。
- [x] M3.4 迁移 SSE AI 润色、恢复和错误处理。
  - 完成：复用 `streamSse` 读取 `delta/done/error` 事件，支持流式草稿、恢复原文和失败回退。
- [x] M3.5 覆盖加载、空态、失败和主要交互测试。
  - 完成：前端 14 项测试覆盖空态、加载、API 失败、类型范围、任务筛选、编辑、复制、SSE 润色和恢复。
- [x] M3.6 对照旧版人工验收全部报告类型。
  - 完成：报告类型、周期范围、空态、加载、失败、编辑、复制、下载和 SSE 润色均已通过自动化与本地入口检查。
- [x] M3.7 用户验收 M3。
  - 完成：用户在报告入口可访问后要求继续推进，视为通过 M3 并进入 M4。

## M4：设置面板

- [x] M4.1 迁移 LLM 提供方、模型和连接测试。
  - 完成：React 设置面板接入提供方增删改、默认提供方、模型目录、拉取模型、保存和连接测试。
- [x] M4.2 迁移外观和用户名设置。
  - 完成：保留 `tb-theme` 与 `tb-user-name`，支持三态主题和操作人昵称。
- [x] M4.3 迁移标签管理。
  - 完成：标签加载、新增、编辑、删除、颜色、创建人和创建时间均接入 `/api/tags`。
- [x] M4.4 迁移数据导入导出。
  - 完成：接入 `/api/export` 下载和 `/api/import` 文件恢复，并保留导入前确认与结果反馈。
- [x] M4.5 验证 API Key 遮蔽和设置错误场景。
  - 完成：读取设置只展示 `hasKey/keyTail`，输入框不回显密钥，支持清除已保存密钥，加载/保存/导入错误有可见反馈。
- [x] M4.6 增加主要设置行为测试。
  - 完成：前端 18 项测试通过，覆盖设置加载、密钥遮蔽、主题、昵称、标签、数据入口和错误反馈。
- [x] M4.7 用户验收 M4。
  - 完成：用户要求继续推进，视为通过 M4 并进入 M5。

## M5：任务创建

- [x] M5.1 迁移手动任务创建。
  - 完成：React 入口接入标题、描述、优先级、截止日期、状态、操作人和 `POST /api/tasks`。
- [x] M5.2 迁移 AI 解析和未配置模型引导。
  - 完成：接入 `POST /api/ai/parse`，解析失败时保留错误反馈；未配置模型可直接跳转设置。
- [x] M5.3 迁移草稿编辑、删除和批量入库。
  - 完成：AI 草稿可逐条修改、删除，支持标签编辑并通过 `POST /api/tasks/batch` 入库。
- [x] M5.4 迁移标签选择。
  - 完成：手动任务使用旧版标签选择浮层；AI 草稿按旧版保留逗号分隔标签编辑入口。
- [x] M5.5 覆盖成功、校验失败和请求失败测试。
  - 完成：前端 23 项测试通过，覆盖手动成功、标题校验、创建失败、AI 解析、草稿批量入库和未配置模型引导。
- [ ] M5.6 用户验收 M5。
  - 待办：手动创建和智能创建已在 React 看板入口接通，仍需用户实际确认创建流程。

## M6：看板

- [x] M6.1 迁移六列只读渲染、搜索和标签过滤。
  - 完成：React 看板读取 `/api/tasks`、`/api/tags`，提供六列、搜索、标签筛选、统计和加载/错误状态。
- [x] M6.2 迁移任务详情、编辑和删除。
  - 完成：详情弹窗支持完整字段查看、编辑 PUT、删除确认和列表同步。
- [x] M6.3 迁移评论和历史轨迹。
  - 完成：详情展示评论/创建与移动轨迹，并接入评论新增、回复和删除。
- [x] M6.4 迁移新建入口和空看板引导。
  - 完成：手动/智能创建入口接入 M5 弹窗；空看板显示可关闭的一次性引导。
- [x] M6.5 迁移列内排序和跨列拖拽。
  - 完成：卡片可拖入列或其他卡片前，按列生成有序 ID 并提交 `/api/tasks/reorder`。
- [x] M6.6 迁移阻塞原因和状态时间戳表现。
  - 完成：进入阻塞中时提供原因输入，详情显示开始/完成/取消时间戳。
- [x] M6.7 迁移翻转、悬浮、粒子和减少动态效果行为。
  - 完成：卡片支持光标跟随 3D 倾斜与边缘高光，详情弹窗翻转入场，删除时粒子散开，并对 `prefers-reduced-motion` 关闭动态效果。
- [x] M6.8 覆盖筛选、编辑、删除和拖拽关键行为测试。
  - 完成：前端 33 项测试通过，覆盖看板筛选、卡片指针倾斜、详情编辑/删除/评论回复删除、空态引导、跨列拖拽和阻塞原因。
- [~] M6.9 深色、浅色、鼠标、键盘和减少动态效果人工验收。
  - 进行中：已在本地根路径人工核对深浅主题、卡片悬浮和 Ctrl+2 报告快捷键；减少动态效果已有自动化/CSS 降级验证，待用户最终确认。
- [ ] M6.10 用户验收 M6。
  - 待办：React 看板达到功能与视觉一致后，由用户最终确认。

## M7：根路径切换

- [x] M7.1 让 React 接管 `/`，保留短期旧版回退入口。
  - 完成：生产构建后的 React 从 `/` 提供，`/next` 保持兼容，旧版固定从 `/legacy/` 提供。
- [x] M7.2 验证直接访问、刷新、静态资源和 `/api/*` 错误路径。
  - 完成：已验证 `/`、`/legacy/`、React JS 静态资源、未知 API JSON 404 和浏览器直接访问。
- [x] M7.3 执行完整自动化检查。
  - 完成：后端 71 项、前端 36 项测试通过，生产构建成功。
- [~] M7.4 执行全功能人工回归。
  - 进行中：已完成根路径 React 看板、API 状态和六列任务加载冒烟检查；完整报告、设置、创建、编辑、拖拽和旧版回退仍待人工确认。
- [ ] M7.5 用户验收 M7。
  - 待办：用户批准 React 成为默认前端后，方可进入 M8。

## React 现存问题优化（M8 前置）

- [x] R1 创建任务后同步刷新看板。
  - 完成：手动创建和智能批量创建成功后刷新任务/标签数据，新卡片无需手动刷新即可出现。
- [x] R2 设置变更同步看板。
  - 完成：标签保存、标签删除和数据导入通过现有事件通知看板刷新，筛选项与卡片数据保持同步。
- [x] R3 详情评论交互补齐。
  - 完成：支持评论回复、仅允许当前操作人删除自己的评论，支持请求中状态和错误反馈；评论变更回写看板状态。
- [x] R4 回归验证。
  - 完成：后端 71 项、前端 36 项测试通过，生产构建成功；旧前端清理未开始。
- [x] R5 看板工具栏与创建入口优化。
  - 完成：移除占用任务展示空间的迁移标题区；手动与智能创建统一为一个入口，智能模式保留在创建弹窗内；标签筛选支持选中摘要、清除、外部点击关闭和 Escape 关闭。
- [x] R6 React 开发页与迁移前原生看板布局对齐。
  - 完成：纠正视觉基准后，以迁移前原生 `public/` 页面重新对齐；统计位于 38px 顶栏中央，搜索/标签/新建位于 42px 操作栏右侧；恢复 280px 固定列、状态底色、列内滚动，以及描述、卡片成员、优先级、标签、截止时间、逾期状态、阻塞原因的卡片字段顺序。已通过同视口截图和计算样式核对。
- [~] R7 全层级 UI 与迁移前原生前端对齐。
  - [x] R7.1 报告页恢复旧版顶栏控件、320px 任务侧栏、编辑区、空态与底部操作布局。
  - [x] R7.2 设置窗恢复旧版 860px 面板、168px 分区导航、LLM 折叠提供方、个性化、数据和标签管理布局。
  - [x] R7.3 创建任务恢复旧版 50vw × 80vh 弹窗、单列表单、分段模式与标签选择浮层。
  - [x] R7.4 任务详情/编辑恢复旧版纵向详情、评论线程、轨迹、编辑表单及底部操作区。
  - [x] R7.5 删除、阻塞原因、提供方删除、模型选择、预设、标签与颜色等二级/三级浮层恢复旧版结构和尺寸。
  - [x] R7.6 将报告、创建、编辑和设置中的原生 select 统一替换为旧版自绘下拉，并保留外部点击与 Escape 关闭。
  - [x] R7.7 更新关键交互回归测试；主应用交互 29 项、前端合计 36 项测试通过，生产构建通过；加载、错误、空看板、AI 解析和数据导入等瞬态界面也已按旧版复核。
  - [~] R7.8 全部状态的同视口截图复核；浏览器自动化暂时拒绝继续读取本地页面，待恢复后完成最终逐层截图验收。

## M8：旧前端清理

> 暂停：等待用户明确确认后再开始清理旧前端、`/next` 兼容入口和旧版回退入口。

- [ ] M8.1 删除已替代的原生 JavaScript 文件和脚本引用。
- [ ] M8.2 删除 `window.*` 旧 interface 和临时兼容代码。
- [ ] M8.3 删除重复或无引用样式与资源。
- [ ] M8.4 删除临时 `/next` 和旧版回退入口。
- [ ] M8.5 更新 README、启动、构建和部署说明。
- [ ] M8.6 执行完整自动化与人工回归。
- [ ] M8.7 用户验收 M8。
  - 完成：旧前端完全移除，迁移结束。

## M9：Specular 卡片边缘高光

- [ ] M9.1 用 CSS mask 与 OGL/WebGL 各做一个受限性能原型。
  - 完成：在真实卡片数量和拖拽场景记录视觉差异、帧率和资源占用。
- [ ] M9.2 选择最小可行实现并记录理由。
- [ ] M9.3 实现悬停、键盘聚焦和减少动态效果行为。
- [ ] M9.4 验证深浅主题、拖拽、卡片动效和多卡片性能。
- [ ] M9.5 用户验收 M9。
  - 完成：高光达到期望且不影响看板交互。
