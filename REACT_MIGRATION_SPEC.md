# React 渐进迁移规格

> 状态：approved
> 基线：本地 `develop` 的 `2d893cf`（`feat: refine topbar layout`）
> 执行清单：[REACT_MIGRATION_TASKS.md](./REACT_MIGRATION_TASKS.md)

## 1. 目标

在保持现有功能、数据和 HTTP 行为不变的前提下，把 `public/` 下的原生 JavaScript 前端渐进迁移到 React。迁移期间旧前端始终可用；React 达到完整功能一致后再切换根路径并删除旧实现。

迁移完成后必须满足：

- Express 继续负责本地数据、LLM 调用、报告计算和 `/api/*`。
- 任务、设置和本地存储文件不需要迁移。
- 用户仍通过原 URL 使用应用。
- 现有视觉 token、中文文案、快捷键和浏览器偏好保持兼容。
- 原生前端全局对象被移除，不作为 React 的长期依赖。

## 2. 非目标

本次迁移不包含后端重写、数据库引入、接口字段重命名、TypeScript 改造、UI 重设计或新产品功能。暂不引入 Redux、Zustand、Tailwind、React Router 等非必要依赖。

卡片边缘 Specular 高光是迁移后的独立视觉任务，只在看板行为迁移并验证完成后实施。

### 2.1 当前视觉基准

后续所有 React 视图和弹层布局统一以迁移前的原生前端（`public/`，原运行入口为 [http://127.0.0.1:3301/](http://127.0.0.1:3301/)）为唯一视觉基准。这里的“布局”包括顶栏结构、内容区间距、工具栏排列、控件形态、颜色 token、字体层级、卡片字段与顺序、卡片密度、弹层尺寸和响应式断点。

视觉一致性的范围覆盖全部层级：一级页面、二级/三级及更深层状态、模态窗、确认窗、悬浮窗、自绘下拉、标签/颜色选择器、空态、加载态、错误提示、Toast 与动效；不能只对齐页面静态首屏。

- React 迁移只替换渲染引擎，不主动重构信息架构或视觉设计。
- `public/` 的 DOM、CSS、字段文案和交互状态同时作为行为与视觉参考；React 应在组件边界内复现其可见结果。
- React 独有的视觉或布局优化必须拆为独立任务，并经用户明确批准后实施。

## 3. 当前约束

- Express 同时提供 HTTP 接口、静态资源和 SPA 回退。
- 原生前端通过 `window.BoardApp`、`window.SettingsPanel`、`window.CreateModal` 等全局对象协作。
- `public/board.js` 同时承担加载、渲染、拖拽、编辑、评论、历史和动效，不能由多个 React root 分割接管。
- 当前自动化测试的稳定 seam 是 `/api/*` HTTP 接口，共 69 项，阶段 0 基线全部通过。
- 项目声明 Node.js `>=18`，本地运行时为 Node.js `24.18.0`；当前 Vite 要求 Node.js `20.19+` 或 `22.12+`。
- 建分支时本地 `develop` 比 `origin/develop` 超前一个已确认提交 `2d893cf`。首次发布迁移代码前必须先让远端包含该基线。

## 4. 目标结构

迁移期间采用并行前端：

```text
Express
├── /api/*        现有 HTTP 接口
├── /             现有原生前端
└── /next/*       React 前端
```

迁移完成后：

```text
Express
├── /api/*        原 HTTP 接口
└── /*            React 构建产物
```

React 源码放入 `client/`，构建产物放入被 `.gitignore` 排除的目录。根项目继续使用一个 `package.json` 和一个锁文件，避免形成两个依赖生命周期。

开发时 Vite 提供 React 页面并把 `/api` 代理到 Express；生产时 Express 提供 Vite 构建产物。迁移阶段的 `/` 与 `/next` 必须能并行访问，形成明确回滚点。

## 5. Seam 与 interface

### 5.1 HTTP seam

React 只通过现有 `/api/*` 获取和修改业务数据。迁移不得为了适配 React 改变状态码、响应体、错误语义或 SSE 格式。确有接口缺口时，先单独提出后端变更规格。

### 5.2 React 入口 seam

React 前端只创建一个根节点，并独占该节点下的 DOM。原生代码和 React 不得同时修改同一 DOM 子树，也不为每张任务卡创建独立 React root。

### 5.3 浏览器能力 seam

主题持久化、剪贴板、文件下载、SSE 读取和减少动态效果检测集中在少量模块中。页面模块通过小 interface 使用这些能力，不直接复制底层实现。

### 5.4 样式 seam

迁移初期复用现有 CSS token、类名和字体，优先保证视觉一致。每迁移完一个完整视图，再把该视图的样式所有权移动到 `client/`。同一选择器只能有一个事实源。

## 6. React 实现原则

- 使用 JavaScript + JSX，保持本轮变更聚焦。
- 使用 React Hooks 和就近状态；共享状态只提升到实际共同祖先。
- HTTP 请求的 JSON 错误解析和 SSE 读取各自只实现一次。
- 不为每个 HTTP 端点创建无行为的浅层包装。
- 组件测试通过用户可见行为验证，不断言 hook、state 或 DOM 实现细节。
- 迁移一个完整能力后再删除对应旧实现；不长期维护双写状态。

## 7. 迁移顺序

### M0：规格与基线

建立迁移分支、规格、任务清单和测试基线。此阶段不安装依赖、不修改业务代码。

### M1：React 基础设施

引入 React、Vite、Vitest 和 Testing Library；建立 `/next` 入口、开发代理、生产构建和 Express 静态挂载。旧前端保持原样。

### M2：应用外壳与主题

迁移顶栏、导航、页面容器和三态主题。保持已有 localStorage 键、快捷键、CSS token 和字体资源兼容。

### M3：报告视图

迁移报告类型、日期范围、周期切换、摘要读取、任务勾选、Markdown 编辑、复制、下载、SSE 润色和恢复。报告视图是第一个完整垂直切片。

### M4：设置面板

迁移 LLM 提供方与模型、外观、用户名、标签和导入导出。密钥只保留在服务端，读取设置时不得回显。

### M5：任务创建

迁移手动创建、AI 解析、草稿编辑、批量入库、标签选择和未配置模型时的引导。

### M6：看板

按以下内部顺序迁移，但只在所有子项完成后替换旧看板：

1. 六列只读渲染、搜索和标签过滤。
2. 卡片详情、编辑和删除。
3. 评论和历史轨迹。
4. 新建入口和空看板引导。
5. 列内排序和跨列拖拽。
6. 阻塞原因和状态时间戳表现。
7. 卡片翻转、悬浮、粒子和减少动态效果行为。

### M7：根路径切换

React 接管 `/`；保留一个短期、明确标注的旧版回退入口。数据和 HTTP interface 不变。

### M8：旧前端清理

验证根路径稳定后，删除被替代的原生脚本、`window.*` interface、重复样式和临时 `/next` 入口，更新运行说明。

### M9：Specular 卡片边缘高光

在 React 看板稳定后，将高光实现为任务卡的视觉层。默认只在悬停或键盘聚焦卡片上运行，尊重 `prefers-reduced-motion`，并验证多卡片场景的帧率和拖拽行为。是否采用 CSS mask 或 OGL/WebGL，以性能原型结果决定。

## 8. 验证策略

### HTTP 行为

现有 Node test runner 测试继续作为后端回归门槛。任何阶段都必须保持 69 项基线测试通过；若测试总数增加，以零失败为门槛。

### React 行为

Vitest 和 Testing Library 覆盖用户可见行为：加载、空态、输入、提交、错误、成功和状态切换。通过可替换的 `fetch` 或浏览器能力 adapter 提供测试数据，不连接真实 LLM。

### 构建与人工验收

每个阶段必须通过生产构建。涉及视觉、拖拽、剪贴板、下载或动画时，补充深色/浅色人工验收；看板迁移还需验证鼠标、键盘和减少动态效果模式。

建议最终统一检查命令为：

```sh
npm run check
```

该命令在 M1 中定义，并至少串行执行后端测试、React 测试和生产构建。

## 9. 阶段完成条件

一个阶段只有同时满足以下条件才算完成：

- 该阶段任务清单全部勾选。
- 自动化检查全部通过。
- 用户可见行为按验收步骤验证。
- 旧前端仍可使用，或该阶段明确批准删除对应旧实现。
- 工作区没有该阶段造成的无用 import、文件、样式或临时兼容代码。
- 用户已测试、审阅并明确批准后，才允许提交、推送和发起 PR。

## 10. 回滚

- M1-M6：停止提供 `/next` 即回到原生前端；业务数据不受影响。
- M7：把根路径恢复到原生静态入口；React 构建仍可保留用于诊断。
- M8：只在 M7 稳定并获批准后执行；删除前以 Git 分支和已合并提交作为恢复点。
- 任一阶段发现 HTTP 行为需要变化时暂停前端迁移，将接口变更拆成独立任务。

## 11. 参考

- [React：向现有项目添加 React](https://react.dev/learn/add-react-to-an-existing-project)
- [Vite：Backend Integration](https://vite.dev/guide/backend-integration.html)
- [Vite：Building for Production](https://vite.dev/guide/build.html)
- [Vite：Getting Started 与 Node.js 要求](https://vite.dev/guide/)
