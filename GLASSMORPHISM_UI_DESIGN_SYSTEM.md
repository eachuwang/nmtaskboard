# 毛玻璃 UI 设计规范

版本：1.0  
适用范围：牛马任务看板 Web 端  
状态：基于现有实现与评审结论整理

## 1. 设计目标

本规范定义毛玻璃主题的视觉语言、设计令牌、组件规格、交互动画与工程约束。目标不是给普通界面覆盖一层透明白色，而是建立稳定、统一且可配置的光学层级。

整体体验应当满足以下关键词：

- 大留白、安静、轻盈、低饱和、克制而高级。
- 接近 Apple Vision Pro 与 iOS 玻璃界面的半透明雾面亚克力质感。
- 背景真实透过玻璃并被柔焦，而不是灰色遮罩或纯白色块。
- 玻璃具有极细高光边缘、轻微厚度、自然折射和柔和空气感阴影。
- 信息层级清晰，文字在任何背景与透明度下均可读。
- 动画过程中与静止状态使用同一套玻璃材质，不能发生亮度、透明度或填充突变。

### 1.1 设计原则

1. **背景负责提供景深**：背景位于最底层；默认使用纯净浅灰色，可选用户本地图片，不使用动态渐变或复杂纹理。
2. **玻璃负责承载界面**：窗口、卡片、控件、按钮和输入框悬浮于背景之上，按层级使用不同强度的同源玻璃。
3. **内容永远位于材质之上**：文字、图标和交互状态不得被滤镜层覆盖。
4. **同类组件使用同一材质**：不能混用纯白填充、旧版彩色填充动画和玻璃按钮。
5. **透明不等于低对比度**：背景可以透出，但正文、状态与焦点必须保持明确。
6. **动画改变几何，不更换材质**：翻转、放大与回收过程中只插值位置、形状、背景遮罩和动态模糊，不切换到临时白底。

## 2. 层级模型

界面从后到前固定为六层：

| 层级 | 名称 | 用途 | 典型元素 |
| --- | --- | --- | --- |
| 0 | Background | 提供色彩、景深与背景内容 | 浅灰画布、用户背景图片 |
| 1 | Strong Glass | 最高承载能力的悬浮玻璃 | 顶部导航、设置窗、详情窗、弹出菜单 |
| 2 | Surface Glass | 常规悬浮玻璃 | 任务卡片、报告编辑区、筛选板块 |
| 3 | Inset Glass | 嵌入上层玻璃内部的低对比区域 | 详情信息区、评论、设置分组、提供方条目 |
| 4 | Control Glass | 小尺寸可交互玻璃 | 按钮、输入框、选择器、图标按钮 |
| 5 | Content | 信息与交互反馈 | 文字、图标、边框、焦点环 |

同一位置最多保留一次真实背景模糊。嵌套面板以透明叠色、高光和边框建立层次，避免多层 `backdrop-filter` 叠加造成发白、发灰和性能下降。

## 3. 设计令牌架构

设计令牌使用三层结构，新增样式应从上到下引用，不在组件中散落无语义色值。

```text
Primitive（原始值）
  └── Semantic（用途）
        └── Component（组件局部）
```

- **Primitive**：颜色、字号、间距、圆角、模糊、阴影等不可再分的值。
- **Semantic**：正文、危险、表面、玻璃边框、状态色等与使用目的相关的值。
- **Component**：任务卡片、弹窗、按钮等组件对语义令牌的映射。

现有 CSS 变量可按此结构理解，例如：

```css
/* Primitive */
--glass-blur-amount: 22px;

/* Semantic */
--glass-surface-bg: var(--glass-specular),
  rgb(var(--glass-surface-rgb) / var(--glass-opacity));

/* Component */
.board-card {
  --glass-local-bg: var(--glass-surface-bg);
}
```

## 4. 字体与排版

### 4.1 字体族

```css
--font-sans: "Geist Sans", -apple-system, BlinkMacSystemFont,
  "Segoe UI", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei",
  "Helvetica Neue", Helvetica, Arial, sans-serif;

--font-mono: "Geist Mono", "SF Mono", "JetBrains Mono", "Fira Code",
  Consolas, "Liberation Mono", Menlo, Courier, "PingFang SC",
  "Microsoft YaHei", monospace;
```

- 中文与正文统一使用无衬线字体。
- 数字启用 `font-variant-numeric: tabular-nums`，确保日期、数量和时间对齐。
- 使用可变字体字重 100–900；页面启用抗锯齿渲染。
- 等宽字体只用于短标签、代码、标识符，不用于大段正文。

### 4.2 字号层级

| 令牌 | 字号 / 行高 | 字重 | 字间距 | 用途 |
| --- | --- | --- | --- | --- |
| Display | 20 / 28px | 650 | -0.01em | 引导页主标题、重要空状态 |
| Dialog Title | 14 / 22px | 560 | 0.025em | 弹窗、设置页标题 |
| Card Title | 13 / 21px | 570 | 0.018em | 任务卡片标题 |
| Body | 13px / 1.6 | 400–470 | 0.018em | 正文、详情内容 |
| Navigation | 12 / 18px | 470；选中 590 | 0.055em | 顶部导航 |
| Section Label | 12 / 18px | 570 | 0.045em | 状态列标题、分区标题 |
| Button | 12 / 18px | 500–560 | 0.018em | 常规按钮、选择器 |
| Metadata | 11 / 18px | 400–500 | 0.025em | 卡片描述、日期、辅助信息 |
| Tag | 10 / 14px | 500 | 0.02em | 卡片标签 |
| Eyebrow | 10 / 14px | 600 | 0.16em | 极短的英文或编号眉题，可选 |

规则：

- 毛玻璃主题全局基准字号为 13px，字间距为 `0.008em`。
- 中文界面不使用过大的字距；只在短导航、状态标题和英文眉题中增强字距。
- 正文优先使用 400–470 的细字重，标题通过 560–590 建立层级，不使用大面积粗黑体。
- 字号不得小于 10px；承载操作或关键信息的文字不得小于 11px。

## 5. 颜色系统

### 5.1 浅色毛玻璃主题

| 语义 | 色值 |
| --- | --- |
| 主文字 | `#1B1E20` |
| 次文字 | `#596269` |
| 三级文字 | `#737D83` |
| 说明文字 | `#939CA2` |
| 强调色 | `#607B96` |
| 强强调色 | `#7896B3` |
| 强调浅底 | `#E2E9EE` |
| 成功 | `#5D806E` |
| 警告 | `#A77E47` |
| 危险 | `#B45F5F` |
| 悬浮叠色 | `rgba(61, 76, 84, 0.055)` |
| 按下叠色 | `rgba(61, 76, 84, 0.095)` |
| 默认画布 | `#EEF1F2` |

### 5.2 深色毛玻璃主题

| 语义 | 色值 |
| --- | --- |
| 主文字 | `#F0F2F3` |
| 次文字 | `#C1C7CA` |
| 三级文字 | `#A0A8AD` |
| 说明文字 | `#828C92` |
| 强调色 | `#8DA6BB` |
| 强强调色 | `#A2B8CA` |
| 强调浅底 | `#303C45` |
| 成功 | `#82A692` |
| 警告 | `#C19B68` |
| 危险 | `#D18484` |
| 悬浮叠色 | `rgba(255, 255, 255, 0.065)` |
| 按下叠色 | `rgba(255, 255, 255, 0.11)` |
| 默认画布 | `#111315` |

### 5.3 任务状态主题色

状态色用于列圆点、数量玻璃、卡片染色和相关文字。卡片仍属于同一种玻璃，只做低饱和染色，不改成实色卡片。

| 状态 | 色值 | 视觉倾向 |
| --- | --- | --- |
| 待规划 | `#846378` | 灰紫 |
| 待办 | `#457097` | 静蓝 |
| 进行中 | `#347D74` | 青绿 |
| 阻塞中 | `#9F6F2F` | 琥珀棕 |
| 已完成 | `#376F4A` | 深绿 |
| 已取消 | `#786068` | 灰玫瑰 |

任务卡片染色公式：

```css
background:
  linear-gradient(
    145deg,
    rgb(var(--status-rgb) / 0.234),
    rgb(var(--status-rgb) / 0.072) 62%,
    transparent
  ),
  var(--glass-surface-bg);
```

- 默认边框混入 18% 状态色，悬浮时提高到 32%。
- 状态圆点为 8 × 8px，使用状态实色，并带 `0 0 8px rgb(status / 0.2)` 柔光。
- 数量标记为 20 × 20px 圆形玻璃，状态色只占约 16%，不能使用白色实底。
- 不依赖颜色单独传递状态，必须同时保留状态名称或其他文字信息。

## 6. 背景规范

### 6.1 默认背景

- 最终默认方案为纯净浅灰画布 `#EEF1F2`，深色模式为 `#111315`。
- 可以加入不超过 2% 不透明度的极细微噪点，避免大面积数字色带；不得形成可辨认纹理。
- 不使用 Grainient、Vanta 或其他动态着色背景作为默认效果。
- 顶部导航与主内容共享同一背景层，页面之间不得出现水平分界线或第二块背景。

### 6.2 用户背景图片

- 用户可以在主题设置中上传本地背景图片，最大 2MB。
- 图片仅保存在本地，使用 `center / cover / no-repeat`。
- 上传图片不得改变玻璃的语义层级；可读性不足时，应提高玻璃不透明度或模糊强度。
- 移动端与桌面端均使用覆盖裁切，不拉伸图片。

## 7. 毛玻璃材质参数

### 7.1 用户可调参数

毛玻璃主题在“主题”选项中独立开启。只有开启后才展开详细设置。

| 设置 | 默认值 | 范围 | 说明 |
| --- | --- | --- | --- |
| 开启毛玻璃 | 关闭 | 开 / 关 | 关闭时使用普通主题 |
| 玻璃透明度 `T` | 58% | 10%–80% | 数值越高，背景越清晰 |
| 背景模糊 | 22px | 0–32px | 数值越高，背景越柔和 |
| 背景图片 | 无 | 最大 2MB | 本地图片，可移除 |

设置中的“玻璃透明度”表示背景透出比例，基础玻璃色的实际不透明度为：

```text
玻璃表面不透明度 A = 1 - 用户透明度 T
默认：A = 1 - 0.58 = 0.42
```

不要把设置中的 58% 直接写成白色填充的 58% 不透明度，否则玻璃会明显发白。

### 7.2 浅色基础光学参数

```css
--glass-opacity: 0.42;
--glass-blur-amount: 22px;

--glass-surface-rgb: 249 251 252;
--glass-strong-rgb: 252 253 253;
--glass-accent-surface-rgb: 232 238 241;

--glass-blur:
  blur(var(--glass-blur-amount)) saturate(118%) brightness(1.025);
--glass-control-filter:
  blur(var(--glass-blur-amount)) saturate(114%) brightness(1.02);

--glass-border: rgba(255, 255, 255, 0.66);
--glass-border-subtle: rgba(255, 255, 255, 0.42);
--glass-control-border: rgba(255, 255, 255, 0.60);
```

### 7.3 材质强度公式

所有玻璃层共享 `A`，通过钳制函数生成层级差异：

```css
/* 常规表面 */
rgb(var(--glass-surface-rgb) / var(--glass-opacity));

/* 强玻璃：默认 0.58 */
rgb(var(--glass-strong-rgb) / clamp(0.28, calc(var(--glass-opacity) + 0.16), 0.88));

/* 内嵌玻璃：默认从 0.22 渐变到 0.04 */
rgb(var(--glass-surface-rgb) / clamp(0.12, calc(var(--glass-opacity) - 0.20), 0.54));
rgb(var(--glass-accent-surface-rgb) / clamp(0.04, calc(var(--glass-opacity) - 0.42), 0.24));

/* 控件玻璃：默认从 0.26 渐变到 0.06 */
rgb(var(--glass-surface-rgb) / clamp(0.16, calc(var(--glass-opacity) - 0.16), 0.60));
rgb(var(--glass-accent-surface-rgb) / clamp(0.06, calc(var(--glass-opacity) - 0.38), 0.30));
```

### 7.4 高光与折射

常规玻璃使用极淡斜向镜面反射，不使用明显渐变装饰：

```css
--glass-specular: linear-gradient(
  148deg,
  rgba(255, 255, 255, 0.30) 0%,
  rgba(255, 255, 255, 0.08) 34%,
  rgba(218, 228, 232, 0.05) 66%,
  rgba(255, 255, 255, 0.16) 100%
);

--glass-specular-strong: linear-gradient(
  148deg,
  rgba(255, 255, 255, 0.34) 0%,
  rgba(255, 255, 255, 0.10) 38%,
  rgba(218, 228, 232, 0.06) 68%,
  rgba(255, 255, 255, 0.18) 100%
);
```

- 高光角度在同一页面保持一致，默认 148°。
- 高光只负责表现镜面与厚度，不得成为肉眼明显的彩色渐变。
- 深色模式将高光不透明度控制在约 2.5%–11%。

### 7.5 边缘与阴影

```css
--glass-highlight:
  inset 0 1px 0 rgba(255, 255, 255, 0.82),
  inset 1px 0 0 rgba(255, 255, 255, 0.28),
  inset 0 -1px 0 rgba(96, 114, 122, 0.07);

--glass-shadow:
  0 18px 50px rgba(67, 82, 90, 0.10),
  0 5px 16px rgba(67, 82, 90, 0.055),
  0 1px 2px rgba(67, 82, 90, 0.04);

--glass-inset-shadow:
  0 12px 32px rgba(67, 82, 90, 0.065),
  0 2px 8px rgba(67, 82, 90, 0.035);

--glass-modal-shadow:
  0 32px 90px rgba(49, 62, 69, 0.18),
  0 8px 26px rgba(49, 62, 69, 0.09);
```

阴影应表现空气感和悬浮高度，不使用边缘坚硬、纯黑或大范围发灰的阴影。

### 7.6 深色材质

```css
--glass-surface-rgb: 29 32 35;
--glass-strong-rgb: 34 37 40;
--glass-accent-surface-rgb: 51 58 62;

--glass-border: rgba(255, 255, 255, 0.16);
--glass-border-subtle: rgba(255, 255, 255, 0.095);
--glass-control-border: rgba(255, 255, 255, 0.14);

--glass-shadow:
  0 22px 58px rgba(0, 0, 0, 0.34),
  0 6px 18px rgba(0, 0, 0, 0.21),
  0 1px 2px rgba(0, 0, 0, 0.18);

--glass-modal-shadow:
  0 36px 100px rgba(0, 0, 0, 0.52),
  0 10px 30px rgba(0, 0, 0, 0.30);
```

深色模式仍需保留透明度和高光，不能退化为不透明深灰卡片。

## 8. 圆角、间距与尺寸

### 8.1 基础令牌

```css
--radius-card: 16px;
--radius-input: 10px;
--radius-pill: 999px;

--space-1: 2px;
--space-2: 4px;
--space-3: 6px;
--space-4: 8px;
--space-5: 10px;
--space-6: 12px;
--space-7: 14px;
--space-8: 16px;
--space-9: 18px;
--space-10: 20px;
--space-11: 22px;
--space-12: 24px;
--space-14: 28px;
--space-16: 32px;
```

页面优先使用 12–24px 的块间距和 18–28px 的容器内边距，避免为了展示更多信息而压缩留白。

### 8.2 关键组件尺寸

| 组件 | 规格 |
| --- | --- |
| 顶部导航 | 外边距 `14px 16px 6px`；高 48px；水平内边距 14px；圆角 18px |
| 状态列 | 最小宽度 280px；列间零间隙，使用淡出竖线分隔 |
| 状态列头 | `padding: 5px 12px 4px`，保持紧凑并靠近卡片 |
| 任务卡片 | `padding: 14px 16px 15px`；底间距 12px；圆角 16px |
| 设置窗口 | 宽 `min(860px, 90vw)`；高 `min(76vh, 680px)`；圆角 16px |
| 设置导航 | 桌面端宽 168px；移动端转为横向 |
| 任务详情窗 | 宽 50vw，最小 460px，最大 760px；高 80vh；圆角 16px |
| 常规控件 | 最小高 32px；水平内边距 9–12px；圆角 8–10px |
| 图标按钮 | 视觉容器 28 × 28px；圆形 |

## 9. 组件规范

### 9.1 顶部导航

- 使用 Strong Glass，必须完整悬浮于统一背景之上。
- 高 48px，不再额外占用第二条纯色横条。
- 导航文字为 12/18px；默认字重 470，当前项 590。
- 搜索框、筛选器、新建按钮和设置按钮均使用 Control Glass。
- 顶部区域不得用底边线将背景切成两块。

### 9.2 状态列

- 列本身保持透明，使统一背景连续可见。
- 相邻列之间使用两端淡出的 1px 竖线：中部可见、上下逐渐透明。
- 状态标题用对应状态色圆点、文字标题和圆形数量玻璃共同表达。
- 标题行需比卡片更轻，不使用大面积玻璃底板。

### 9.3 任务卡片

- 使用 Surface Glass 与状态低饱和染色。
- 标题为 13/21px、570；元信息为 11/18px。
- 悬浮时提升阴影和状态边框，几何位移由外部动画容器负责。
- 原卡片被复制到悬浮层后，源位置必须保持空白；打开、关闭和回收全过程不得出现双卡片或双层文字。
- 内容区不得使用独立纯白背景。

### 9.4 弹窗与设置窗口

- 外层使用 Strong Glass，内部分组使用 Inset Glass。
- 弹窗标题 14/22px、560；正文 13px、行高 1.6。
- 详情信息、评论和轨迹区不能叠加第二次背景模糊；使用透明叠色、边框和内阴影建立层级。
- 设置左侧导航必须保持玻璃连续性，不使用白色实底侧栏。
- 设置卡片、提供方条目、删除按钮和标签悬浮态均遵循玻璃控件规则。

### 9.5 按钮

- 毛玻璃主题下所有按钮使用统一的 Control Glass 动效。
- 禁用原有实色填充扩散或径向填充动画。
- 悬浮：`translateY(-1px)`，轻微提高高光、边框与阴影。
- 按下：`translateY(0) scale(0.98)`。
- 危险按钮仍使用透明玻璃，只以危险色控制文字和边框，不填充红色实底。
- 评论中的“回复”和“删除”是同规格文字按钮；删除只在文字悬浮时呈危险色，不显示额外背景形状。
- 禁用态统一 `opacity: 0.45`，同时禁止位移和阴影增强。

### 9.6 输入框与选择器

- 使用 Control Glass，不使用纯白输入框。
- 默认高 32px，圆角 10px，正文 12–13px。
- 聚焦时使用强调色边框和 `0 0 0 3px rgb(accent / 0.18)` 焦点环。
- Placeholder 使用说明文字色；不可只靠低不透明度制造禁用态。

### 9.7 标签胶囊

- 标签不使用背景色，只保留 1px 边框和文字。
- 边框与文字使用用户为对应标签保存的主题色。
- 卡片内标签为 10/14px、`padding: 1px 7px`、圆角 10px。
- 筛选标签可用 11/18px 与胶囊圆角 999px。
- 标签颜色必须满足文字对比度；过浅的用户色需要自动压暗或在深色模式中提亮。

## 10. 图标规范

### 10.1 造型

- 界面图标统一使用线性图标，不混用填充、多色、拟物或 Emoji 图标。
- 默认 `viewBox="0 0 16 16"`，`fill="none"`，`stroke="currentColor"`。
- 线宽为 1.3–1.6px，使用圆头与圆角连接。
- 相同功能只使用一个图形隐喻，例如关闭始终为同一规格的叉号。

### 10.2 尺寸

| 尺寸 | 用途 |
| --- | --- |
| 12px | 下拉箭头、极小辅助图标 |
| 14px | 关闭、回复、删除等次级操作 |
| 16px | 设置、筛选、新建、导航主图标 |
| 22px | 空状态或引导图标 |
| 28px | 图标按钮的最小视觉容器 |

- 图标按钮必须提供可识别的 `aria-label`；SVG 本身使用 `aria-hidden="true"`。
- 图标颜色继承文字语义色，不在 SVG 中硬编码多套颜色。

### 10.3 浏览器 Logo

浏览器 Favicon 是产品标识，可作为多色例外：

- 64 × 64 viewBox，主体轮廓约 58 × 58px，圆角 16px。
- 使用极细白色高光边、低饱和蓝紫光晕与内部图表符号表达玻璃质感。
- 在 16px 显示尺寸下仍需保留清晰轮廓，避免过细的内部细节。

## 11. 动效规范

### 11.1 基础时间与缓动

```css
--ease-standard: cubic-bezier(0.4, 0, 0.2, 1);
--ease-spring: cubic-bezier(0.16, 1, 0.3, 1);
--ease-overshoot: cubic-bezier(0.34, 1.56, 0.64, 1);

--duration-fast: 100ms;
--duration-standard: 200ms;
--duration-morph: 600ms;
```

- 颜色、焦点和轻微高光变化使用 100ms。
- 常规悬浮、缩放和位移使用 200ms。
- 卡片到详情窗的空间变形固定为 600ms。

### 11.2 卡片悬浮

- 外部动画容器使用 `perspective(900px)`。
- 最大倾斜角约 ±15°，最大缩放 1.12。
- 原卡片在悬浮副本存在时用高优先级透明度隐藏，离开后完整清理。
- 玻璃层不参与 3D 变换；动画容器与材质容器必须分离。

### 11.3 卡片翻转方向

- 点击卡片右半侧：保持正向 180° 翻转。
- 点击卡片左半侧：使用反向 -180° 翻转。
- 方向只改变旋转与动态拉伸方向，不改变弹窗布局和最终朝向。

### 11.4 动态模糊

动态模糊遵循“清晰 → 模糊峰值 → 清晰”的连续曲线，并沿翻转运动方向轻微拉伸：

| 进度 | 高斯模糊 | 横向拉伸 |
| --- | --- | --- |
| 0% | 0 | 1 |
| 12% | 0.35px | 1.003 |
| 28% | 2.4px | 1.028 |
| 45% | 1.8px | 1.020 |
| 65% | 1.05px | 1.012 |
| 82% | 0.45px | 1.005 |
| 100% | 0 | 1 |

实现时先旋转到运动轴，再执行 `scaleX`，最后反向旋转回来，避免模糊永远只沿页面水平轴。

### 11.5 背景遮罩插值

详情窗打开和关闭时，背景遮罩必须与 600ms 翻转同步，且使用线性插值：

- 打开：背景模糊从 0 线性增加到 22px；浅色遮罩从透明增加到 `rgba(45, 55, 60, 0.18)`。
- 关闭：背景模糊从 22px 线性减小到 0；遮罩透明度同步降为 0。
- 深色终点遮罩为 `rgba(3, 6, 8, 0.32)`。
- 不得在最后一帧挂载或卸载模糊层，否则会出现突然变暗、变亮、变清晰或变模糊。

### 11.6 动画材质一致性

翻转过程与最终卡片或弹窗必须引用同一组：

- `--glass-opacity`
- `--glass-blur-amount`
- 表面 RGB
- 高光渐变
- 边框
- 阴影

描述信息区、评论区与按钮在动画期间不得切换为纯色填充。真实详情窗和形变容器应始终处于同一个详情遮罩作用域内，避免 CSS 变量作用域变化造成最后一帧材质跳变。

## 12. 工程实现约束

### 12.1 伪元素隔离

所有可动画的玻璃组件使用伪元素承载材质：

```css
.glass-surface {
  position: relative;
  isolation: isolate;
  background: transparent;
}

.glass-surface::before {
  content: "";
  position: absolute;
  inset: 0;
  z-index: -1;
  border-radius: inherit;
  background: var(--glass-local-bg, var(--glass-surface-bg));
  box-shadow: var(--glass-highlight), var(--glass-shadow);
  -webkit-backdrop-filter: var(--glass-blur);
  backdrop-filter: var(--glass-blur);
}
```

强制规则：

- 不在带 `transform`、`will-change: transform` 或 3D 翻转的节点上直接设置 `backdrop-filter`。
- 变换由外层 wrapper 负责，玻璃由内层元素或 `::before` 负责。
- 内容位于伪元素之上，不能依赖负层级穿出父级堆叠上下文。
- 同一组件在静止、悬浮、翻转和弹窗状态下使用相同的材质变量。

这是避免 Chromium 在 3D 合成层中出现白块、闪烁、重影和透明度突变的核心约束。

### 12.2 降级方案

不支持 `backdrop-filter` 时使用近似实色表面：

| 材质 | 浅色回退 | 深色回退 |
| --- | --- | --- |
| Surface | `#F7F8FA` | `#2C2C2E` |
| Strong | `#FFFFFF` | `#303034` |
| Inset | `#EEF2F7` | `#292A2D` |

```css
@supports not ((backdrop-filter: blur(1px)) or
  (-webkit-backdrop-filter: blur(1px))) {
  .glass-surface::before {
    background: var(--glass-fallback);
  }
}
```

## 13. 响应式规范

- 720px 以下隐藏非必要统计和右侧辅助信息，顶部栏水平内边距减为 8px。
- 设置页改为纵向结构，左侧导航变为横向滚动；内容内边距降为 14px。
- 600px 以下任务详情遮罩内边距为 10px，详情信息改为单列，正文区域内边距为 16px。
- 看板状态列仍保持最小宽度 280px，以横向滚动承载，不把卡片压缩成难以阅读的小列。
- 用户背景图持续使用 cover 裁切，避免随屏幕比例拉伸。

## 14. 无障碍规范

- 正文对比度至少 4.5:1；大号文字与 UI 边界至少 3:1。
- 键盘焦点统一使用 2px 强强调色轮廓，外偏移 2px。
- 状态不能只靠颜色表达；图标按钮不能只靠图形而缺少无障碍名称。
- `prefers-reduced-motion: reduce` 时取消入场、悬浮、翻转和形变动画，过渡时长降至近乎 0。
- 未来应支持 `prefers-reduced-transparency`：关闭背景模糊并提高表面不透明度。
- `prefers-contrast: more` 时提高边框与文字对比度，减少背景透出。
- 用户背景图片不能覆盖内容可读性；透明度设置必须始终允许恢复到安全默认值。

## 15. 禁止事项

- 禁止以 `rgba(255, 255, 255, 0.6)` 作为所有组件的统一白色底板。
- 禁止在 3D 变换节点上直接应用 `backdrop-filter`。
- 禁止翻转中使用临时纯白卡片，结束后再突然切换为玻璃。
- 禁止同一区域重复堆叠多个背景模糊层。
- 禁止毛玻璃按钮继续使用旧版彩色填充扩散动画。
- 禁止标签胶囊使用实色背景；标签只保留用户主题色边框与文字。
- 禁止复杂动态背景、花哨纹理、高饱和大渐变和廉价塑料质感。
- 禁止用极低透明度文字补偿背景复杂度。
- 禁止关闭详情窗时提前恢复源卡片，导致回收动画与原卡片重叠。

## 16. 验收清单

### 视觉

- [ ] 页面背景连续，顶部与主内容之间没有分界线。
- [ ] 顶栏、卡片、弹窗、设置导航、按钮和输入框均有一致的玻璃语言。
- [ ] 没有明显白色实底、灰色遮罩感或多层发白。
- [ ] 状态色肉眼可区分，但饱和度克制。
- [ ] 卡片与弹窗的文字、边框和高光在浅色、深色及用户背景下清晰可读。

### 交互

- [ ] 所有按钮使用统一的玻璃悬浮与按下动效。
- [ ] 左右点击卡片会产生相反的翻转方向。
- [ ] 动态模糊连续地清晰—模糊—清晰，并沿翻转方向拉伸。
- [ ] 打开和关闭详情时，背景模糊与遮罩在完整 600ms 内线性插值。
- [ ] 翻转期间描述、评论、标签和按钮不出现纯色填充或亮度跳变。
- [ ] 悬浮或回收期间源卡片位置为空，不出现双卡片、双层文字或重影。

### 工程

- [ ] 可动画节点与 `backdrop-filter` 节点已分离。
- [ ] 所有玻璃表面都具有不支持滤镜时的回退色。
- [ ] 透明度、模糊、深浅主题和用户背景图均通过设计令牌驱动。
- [ ] 降低动态效果时功能完整且无布局跳变。
- [ ] 键盘、屏幕阅读器和高对比度场景均可完成主要操作。

## 17. 实践结论

本项目最终采用原生 CSS 毛玻璃和 React 状态编排，不依赖通用 Glassmorphism 组件库。原因是玻璃参数需要与用户主题设置、任务状态染色、3D 翻转、遮罩插值和 Chromium 合成层规避策略紧密协作，通用组件的封装边界反而容易造成材质不一致。

最重要的工程结论有三点：

1. 玻璃是一个独立材质层，应由隔离的伪元素承载。
2. 形变动画与玻璃采样必须分层，动画期间不能更换材质作用域。
3. 背景、表面、内嵌区域和控件使用同源参数建立层级，任何局部纯白填充都会破坏整体玻璃质感。
