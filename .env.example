# Compose / 服务器部署用。本机双击 start.cmd 或 npm start 时不必填写，程序会自动生成。
BOOTSTRAP_TOKEN=

# 仅使用字母和数字，避免 @ : / 等字符破坏 DATABASE_URL
POSTGRES_PASSWORD=

# 对外端口，容器内仍是 3301
PORT=3301

# HTTP 保持 false；仅在 HTTPS 反代后改为 true
SESSION_SECURE=false
